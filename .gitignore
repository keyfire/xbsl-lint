# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Локальная копия артефактов дистрибутива 1С:Элемент (вендорные файлы не коммитим)
.refs/

# Данные о языке/типах, извлекаемые из дистрибутива, генерируются локально (не публикуем)
xbsl/data/element/
demo/.vscode/

# Нативная сборка mypyc (XBSL_MYPYC=1): бинарники не коммитятся
*.pyd
*.so

# Сайт документации (blume.config.ts): зависимости, генерируемый Astro-проект и
# собранный вывод — не коммитим (dist/ уже покрыт правилом выше).
node_modules/
.blume/

# Инструкции для ИИ-агентов – локальные, в публичный репозиторий не публикуем
CLAUDE.md

# Рабочие заметки по конструктору (спека, план, бэклог задач) – ведём локально, не публикуем
DESIGNER_BACKLOG.md
